<!-- gen by doubao -->

# 二维码数据结构需求说明

## 一、总体要求

- 二维码数据库**暂时硬编码**，数据保存在代码中（变量 `QR_DB`）。
- 数据库内容**不显示在用户界面**，仅用于程序内部读取。
- 数据支持自定义：二级选择项显示名称（label）、二维码卡片标题（title）均可按编码单独设置。

---

## 二、数据结构（两级）

数据为「分类 → 编码」两级结构：

```
QR_DB
 └── 分类1 (category)
      └── 编码列表 (items)
           ├── 编码 A (code)
           ├── 编码 B (code, 可选 label, 可选 title)
           └── ...
 └── 分类2 (category)
      └── ...
```

- **一级：分类（category）** —— 字符串，展示在右侧一级选择列。
- **二级：编码（items）** —— 该分类下所有二维码编码组成的数组。

---

## 三、字段定义

| 字段 | 必填 | 类型 | 说明 |
|------|------|------|------|
| `category` | 是 | 字符串 | 一级分类名，显示在右侧一级选择列 |
| `items` | 是 | 数组 | 该分类下所有二维码编码的列表 |
| `code` | 是 | 字符串 | 编码，**同时是二维码扫码内容**（严格等于此值） |
| `label` | 否 | 字符串 | 二级选择按钮的**显示名称**；不设置时显示 `code` |
| `title` | 否 | 字符串 | 二维码卡片标题；不设置时自动生成 |

---

## 四、核心规则

1. **二维码内容与所选编码完全一致**
   - 选中 `D1217`，扫码内容就是 `D1217`。
   - 不得添加任何多余内容（如"房间类D1217"、"二维码D1217"、"d1217"等均为错误）。

2. **二级选择按钮显示规则（label）**
   - 编码设置了 `label` → 按钮显示该自定义名称。
   - 编码未设置 `label` → 按钮显示 `code`。
   - **label 只影响按钮显示，不影响扫码内容**（扫码内容始终是 `code`）。
   - 示例：`{ code: "XY-KZ-458", label: "自控主机A" }` → 按钮显示「自控主机A」，扫码内容「XY-KZ-458」。

3. **二维码标题生成规则（title）**
   - 编码设置了 `title` → 标题显示该自定义标题。
   - 编码未设置 `title` → 标题自动为「分类名 + 序号」，序号从 1 开始，按数组内顺序排列。
   - 示例：`房间` 分类第 1 个编码 → 标题「房间1」。

4. **序号按数组顺序**，不依赖编码文本本身。

---

## 五、格式示例

```js
var QR_DB = [
  {
    category: "房间",                             // 一级分类名
    items: [                                      // 该分类下的编码列表
      { code: "D1217", title: "离心间" },        // 只有 title：按钮显示 D1217，标题显示 离心间
      { code: "D1218", title: "蛋白提取间" },
      { code: "D1216" }                          // 最小写法：按钮显示 D1216，标题自动为 房间3
    ]
  },
  {
    category: "自控系统",
    items: [
      { code: "XY-KZ-458", label: "自控主机A" }, // 只有 label：按钮显示 自控主机A，扫码内容 XY-KZ-458
      { code: "XY-KZ-457", label: "自控主机B" }
    ]
  },
  {
    category: "仪器",
    items: [
      { code: "ph", label: "pH计", title: "pH计" } // label + title 同时设置
    ]
  }
];
```

**字段组合说明：**

| 场景 | 按钮显示（label） | 二维码卡片标题（title） | 扫码内容（code） |
|------|------|------|------|
| 只写 code | 显示 code | 「分类名 + 序号」 | code |
| code + label | 显示 label | 「分类名 + 序号」 | code |
| code + title | 显示 code | 显示 title | code |
| code + label + title | 显示 label | 显示 title | code |

---

## 六、当前数据规模

- 分类数：**9 个**（房间、反应罐、缓冲液罐、缓冲液流量计、配液罐、离心机、仪器、CIP、自控系统）
- 编码总数：**54 个**
- 带自定义标题（title）：**42 个**
- 带自定义按钮显示名（label）：**2 个**
  - XY-KZ-458 → 自控主机A
  - XY-KZ-457 → 自控主机B

---

## 七、一句话概括

**「分类」下挂「编码」：`code` 决定扫码内容，可选的 `label` 决定二级按钮显示名称，可选的 `title` 决定二维码卡片标题；未设置 `label`/`title` 时分别回退为编码 / 「分类名 + 序号」。**

